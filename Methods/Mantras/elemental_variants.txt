Elemental variants are
 vowel changes(A->i) that change the 
  flow channel in the mantra:
  Example:
  KRIM(water)-> KRUM(ether)
  All variants are using KR-x-M template
  which defines the character of mantra,
  with vowel defining the elemental channel.
